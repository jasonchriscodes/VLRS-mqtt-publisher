package com.jason.publisher

object Constant {
    // Key for storing and retrieving bus data.
    val busDataKey = "busData"

    // Key for storing and retrieving the name of the device.
    val deviceNameKey = "nameDevice"

    // Key for storing and retrieving the access token.
    val tokenKey = "accessToken"

    // Key for storing and retrieving the aid.
    val aidKey = "aid"

    // Key for accessing the shared preferences.
    val sharedPrefsKey = "mySharedPreferences"
}
